# AI作業Handoff

- 担当AI: Claude（この作業を最後に GPT-5.6 sol へ引き継ぐ）
- Role: test / diagnosis
- Task: プロンプト削減後の再計測と、これまでの診断の訂正
- Started At: 2026-07-29 23:00 JST
- Finished At: 2026-07-29 23:30 JST
- Project Root: `C:\Users\coala\Desktop\Grok_API\Codex\AItuber`
- Work Lock: Codexは上限のため停止中。単独編集

## 目的

常時ONのプロンプトを 3642 → 2899tok（−743 / 20%）にした状態で台本を再走し、
冒頭ハッシュが割れるかどうかで効果を判定する。

## 結果

**割れなかった。4回目も 8/8 一致。**

```
Planner有効1回目   a4b5 2497 2f4d 1d5e a4b5 a1c8 ab06 3cf3
Planner有効2回目   a4b5 2497 2f4d 1d5e a4b5 a1c8 ab06 3cf3
Planner無効        a4b5 2497 2f4d 1d5e a4b5 a1c8 ab06 8884   ← 7/8
−743tok後          a4b5 2497 2f4d 1d5e a4b5 a1c8 ab06 3cf3
```

## 【重要】これまでの診断は誤りだった

21:50 に「systemブロックが5000〜6000tokあるので、モデルはユーザーにではなく
規則の山に応答している」と書いた。**これは間違いである。**

`data/dialogue_neuro.json` の `recent_replies`（アプリ自身が保持している直近の
返答）で実際の書き出しを確認した。第12条により本文はここへ複製しないが、
性質だけ記す:

- 入力2「うんうん」に対し、**その相槌そのものを話題にした**書き出し
- 入力3「なるほど」に対し、同じく**その語を受けた**書き出し
- 入力6「ランダムに失敗させればいい」に対し、**その案に乗った**書き出し

**定型文ではない。一つ一つ、その入力にきちんと応じている。ポッポは聞いている。**

### 本当の症状

**同じ入力には必ず同じ返答が返る。**

1番「今のAIって話しててもAI感が強いんだよね」と
5番「またAIが『なるほど、それは重要ですね』って言ってる」は、
モデルから見れば**同じ刺激**（AIっぽい喋り方への指摘）である。
だから同じ返答が返る。ユーザーの「1と5がほぼ一緒」はそのとおりで、
かつ**モデルの側から見れば正しい振る舞い**でもある。

つまり「計画が言葉に届かない」のではなく、
**ある入力から言葉へ至る道が1本しかない**。

## 次に確かめること（最優先。ここより先へ進まない）

`llm.temperature = 0.7`、seed 指定なし。それで
**4回 × 8ターン = 32サンプル、プロンプトが743tok違っても、書き出しが一度も
変わらない**。これは温度0.7の挙動ではなく**温度0の挙動**である。

サンプリングが効いていないなら、プロンプトをいくら削っても何も変わらない。
先にそれを確かめる。

```
python tools/check_sampling.py
```

同じ一文を3回送るだけ。会話履歴もペルソナも使わず、アプリを起動したまま
実行できる。4通りを比較する。

| 出た結果 | 意味 | 次の一手 |
|---|---|---|
| どれも「全部同じ」 | サンプリングが効いていない | `llm/openai_compat.py` の `extra_body["options"]` がOpenAI互換層で温度を落としていないか、モデル側の既定に `seed` が無いかを見る |
| temp=1.0 だけ割れる | 0.7 がこのモデルには低すぎる | `config/config.yaml` の `llm.temperature`。`gemma4:12b-it-qat` の公式推奨は temp 1.0 / top_k 64 / top_p 0.95（同梱の `Modelfile` にも記載） |
| ネイティブだけ割れる | OpenAI互換層が temperature を落としている | `/api/chat` へ切り替えるか、`options` に温度を載せる |
| どれも割れる | サンプリングは正常。原因は入力側 | **直近N件の書き出しを本文のままプロンプトへ渡し、別の入り方にさせる**。スタイル名のような抽象語ではなく具体的で検証可能な制約なので、埋もれても効きやすい。`recent_replies` に材料がある |

## 変更

| File | Change | Why |
|---|---|---|
| `tools/check_sampling.py`（新規） | 同じ一文を3回、4通りの経路で送って書き出しがばらつくか見る | 推測でこれ以上いじらないため |
| `docs/SHARED_CHANGELOG.md` | 23:30の記録 + 冒頭に **「現在地」節** を追加 | 引き継ぐ人が台帳を1本読めば現在地・次の一手・禁止事項が分かるように |

## 変更しなかったもの

コード本体。診断が確定するまで手を出さない。

## テスト

コード本体は変更していないため未実行（直前の全体回帰 **1023 passed / 11 subtests** が有効）。

## 残課題

1. `tools/check_sampling.py` の実行と、結果に応じた分岐（上表）
2. `target_length` がプロファイル `interaction_policy.response_length` で
   `long` に固定される（`conversation_planner.py:382-388`）
3. `Mind.build_context` の4700〜5800tok。**1が片付くまで手を出さない**
4. `docs/CODEMAP` へ `dialogue/leakage.py` と `dialogue/plan_metrics.py` を追記

## やってはいけないこと

- **会話エンジン・名前付き行動の追加。** 対照実験が明確に否定している
- **不具合を見つけるたびにプロンプトへ一文足すこと。** 同じ規則が7箇所へ増えた
  実績がある。`tests/test_prompt_rules.py` が常時ON 3000tok を上限に固定している

## 憲法・ADRへの影響

- Constitution amendment required: なし
- ADR added/updated: なし。ただし **「較正されていない物差しで測る」** を
  R-037（テストが実行しないコード）と並ぶ危険として次回R更新時に加えたい
- Architecture/Codemap updated: 未実施（残課題4）

## 秘密・個人情報確認

- `.env`値を記録していない: はい
- raw audio/imageを添付していない: はい
- private transcriptを複製していない: **はい。** 診断のため `recent_replies` を
  読んだが、本文はこの文書にもログにも複製していない（性質のみ記述）

## 次の担当への一文

**プロンプトはもう十分削った。次はサンプリングを疑う番で、
それは `python tools/check_sampling.py` を一度走らせるだけで決着がつく。**
